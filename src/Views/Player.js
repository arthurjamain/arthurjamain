define([
  'lib/joshlib/Layout',
  'lib/joshlib/List',
  'backbone',
  'underscore'
], function(
  Layout,
  List,
  Backbone,
  _
) {
  
  return Layout.extend({
    initialize: function(options) {
      Layout.prototype.initialize.call(options, this);
    
    }
  });

});